import { FilterTopicsPipe } from './filter-topics.pipe';

describe('FilterTopicsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTopicsPipe();
    expect(pipe).toBeTruthy();
  });
});
