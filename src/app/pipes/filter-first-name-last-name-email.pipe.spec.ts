import { FilterFirstNameLastNameEmailPipe } from './filter-first-name-last-name-email.pipe';

describe('FilterFirstNameLastNameEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFirstNameLastNameEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
